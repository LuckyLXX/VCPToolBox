# 火山引擎豆包4.0图像生成插件配置示例
# 复制此文件为 config.env 并填入真实的配置值

# 火山引擎API密钥 (必填)
# 从 https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey 获取
VOLCENGINE_API_KEY=your_volcengine_api_key_here

# 豆包4.0模型ID (可选，默认使用 doubao-seedream-4-0-250828)
VOLCENGINE_MODEL_ID=doubao-seedream-4-0-250828

# 图片处理模式 (可选，默认为 url)
# url: 直接使用API返回的URL (推荐，更快，符合API设计)
# download: 下载图片并保存到本地服务器 (兼容模式)
IMAGE_MODE=url

# 默认图片尺寸 (可选，默认为 2K)
# 支持: 1K, 2K, 4K 或具体像素值如 1024x1024
DEFAULT_SIZE=2K

# 默认水印设置 (可选，默认为 true)
DEFAULT_WATERMARK=true

# 组图生成默认数量 (可选，默认为 4)
DEFAULT_MAX_IMAGES=4



# 以下配置由VCP系统自动设置，无需手动配置
# PROJECT_BASE_PATH=
# SERVER_PORT=
# IMAGESERVER_IMAGE_KEY=
# VarHttpUrl=
# VarHttpsUrl=